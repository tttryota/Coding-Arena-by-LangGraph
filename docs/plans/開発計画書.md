# Obsidian × RAG × LangGraph 理解度チェックシステム 開発計画書

**作成日:** 2026年4月29日

---

## 1. プロジェクト概要

### 1.1 目的

Obsidianの学習ノートを知識ソースとして、RAG + LangGraphで理解度を対話的に検証する個人学習支援システム。目的は**技術理解の深化**（ポートフォリオ目的ではない）。

### 1.2 確定方針

- **LLM**: ローカルLLM（Qwen2.5-32B、Ollama経由）
- **構成**: モノレポ・2言語構成（Python backend + TypeScript frontend）
- **タグ付与**: LLMが自動でタグを生成（Obsidianの既存タグに依存しない）
- **セッション構造**: 1問1答ではなく、ユーザーが納得するまで対話的に継続
- **深掘り**: LLM判断による深掘り + ユーザー起点の深掘りの両方
- **再帰深度**: 前提知識の追跡は最大2段。それ以上はノート復習を案内
- **出題モード**: 新着ノート / トピック指定 / 弱点復習 / ランダム（フロントエンドで選択）

### 1.3 技術スタック

| レイヤー | 技術 |
|:---|:---|
| Backend | Python / FastAPI / LangGraph / LangChain |
| LLM | Qwen2.5-32B（Ollama経由ローカル） |
| Embedding | multilingual-e5-large（ローカル） |
| VectorDB | ChromaDB（ローカル永続化） |
| ファイル監視 | watchdog（Python） |
| Frontend | TypeScript / React / Vite |
| UIコンポーネント | shadcn/ui（Radix UI + Tailwind CSS） |
| 状態管理 | Jotai |
| 起動管理 | docker-compose（backend + frontend + Ollama） |
| 開発ハーネス | Claude Agent SDK + claude -p |
| 第三者レビュー | OpenAI Codex（フォールバック: Claude 2体並列） |

---

## 2. システムアーキテクチャ

### 2.1 全体構成

```
Obsidian Vault
  │ watchdog
  ▼
Ingestion Pipeline
  チャンク分割 → Tagger Agent（LLMタグ付与） → Embedding → ChromaDB
  │
  ▼
LangGraph Agent Workflow（セッション制の対話ループ）
  │
  ▼
FastAPI Backend
  │
  ▼
React Frontend（shadcn/ui + Jotai）
```

### 2.2 ディレクトリ構造

```
obsidian-quiz/
├── backend/
│   ├── pyproject.toml
│   ├── ingestion/           # Vault監視・チャンク分割・タグ付与
│   ├── agents/              # LangGraphエージェント定義
│   ├── api/                 # FastAPIエンドポイント
│   └── db/                  # ChromaDB操作
├── frontend/
│   ├── package.json
│   └── src/                 # React + TypeScript
├── .harness/                # 開発ハーネス設定
│   ├── harness.py           # Agent SDKオーケストレーター
│   ├── review-criteria-common.md
│   ├── review-criteria-backend.md
│   └── review-criteria-frontend.md
├── docs/
│   ├── design.md            # 概要設計書
│   └── spec/                # 機能仕様書（コンポーネント単位）
├── tests/
│   ├── test-cases/          # テストケース定義（日本語markdown）
│   ├── backend/             # pytestコード
│   └── frontend/            # vitestコード
├── logs/                    # ハーネスのイベントログ
├── plan/                    # 実装計画ファイル（人間が書く）
├── docker-compose.yml
└── CLAUDE.md                # Claude Code用静的制約
```

### 2.3 LangGraphエージェントワークフロー

#### ステート定義

```python
class SessionState(TypedDict):
    mode: str                        # "recent" | "topic" | "weakness" | "random"
    topic_tags: List[str]
    retrieved_chunks: List[str]
    questions: List[dict]            # セッション内の問答履歴
    current_question: dict
    understanding_level: str         # "insufficient" | "partial" | "sufficient"
    next_action: str                 # "deep_dive" | "new_question" | "prerequisite" | "end"
    user_answer: str
    user_request: Optional[str]      # ユーザー起点の深掘りリクエスト
    depth: int                       # 再帰の現在深度
    max_depth: int                   # 上限=2
    prerequisite_topic: Optional[str]
```

#### エージェント構成

- **Tagger Agent**: Ingestion時、LLMでチャンクにタグを自動付与
- **Generator**: RAG検索 + 問い生成
- **Evaluator**: 回答評価 + 理解度判定
- **Router**: 次のアクション判断（深掘り / 新規問い / 前提知識サブセッション / 終了）
- **Summarizer**: セッション終了時のサマリー生成
- **Weakness Tagger**: セッション全体の理解度が低いトピックに `#weakness` タグ付与

#### ワークフローグラフ

```
Generator → wait_for_answer → Evaluator → Router
  ▲                                         │
  │          ┌── deep_dive ─────────────────┤
  │          │                              │
  └──────────┤── new_question ──────────────┤
             │                              │
             ├── prerequisite ──→ SubGraph(再帰, depth+1)
             │                      │
             │                      └→ depth >= 2 なら復習案内して終了
             │
             └── end ──→ Summarizer → Weakness Tagger
```

### 2.4 出題モード

| モード | 検索方法 |
|:---|:---|
| `recent` | メタデータの更新日時でフィルタ（直近N日） |
| `topic` | LLM付与タグでフィルタ → ベクトル類似度検索 |
| `weakness` | メタデータの `#weakness` タグでフィルタ |
| `random` | ランダムサンプリング |

### 2.5 APIエンドポイント

```
GET  /topics          # タグ一覧（件数付き）
POST /quiz/start      # セッション開始（mode指定）
POST /quiz/answer     # 回答送信
POST /quiz/deepdive   # ユーザー起点の深掘りリクエスト
POST /quiz/end        # セッション手動終了
GET  /progress        # 進捗データ
GET  /weakness        # 弱点ノート一覧
```

---

## 3. 開発ハーネス設計

### 3.1 全体構成

```
あなた（人間・司令塔）
  │
  ├── CLAUDE.md: 静的制約（全フロー共通）
  │
  ├── Design Flow: 仕様書 + テストケース作成
  │
  ├── Impl Flow: TDD実装（計画ファイル単位）
  │
  └── Review Flow: セルフ → Codex/2体 → 人間
      │
      └── 全て Agent SDK + claude -p で実行
```

### 3.2 CLAUDE.md（静的制約）

全フローが参照する共通ルール。手順ではなく制約のみ記述する。

```markdown
# プロジェクト概要
Obsidian × RAG × LangGraph 理解度チェックシステム。
個人学習用。技術理解の深化が目的。

# アーキテクチャ
- backend/: Python (FastAPI + LangGraph + LangChain)
- frontend/: TypeScript (React + Vite + shadcn/ui + Jotai)
- docker-compose.yml で統合起動

# 技術制約
- LLM: Qwen2.5-32B（Ollama経由ローカル）
- Embedding: multilingual-e5-large（ローカル）
- VectorDB: ChromaDB（ローカル永続化）
- LangGraphのステートは SessionState を基本とする

# ディレクトリ規約
- backend/ingestion/  Vault監視・チャンク分割・タグ付与
- backend/agents/     LangGraphエージェント定義
- backend/api/        FastAPIエンドポイント
- backend/db/         ChromaDB操作
- frontend/src/       Reactコンポーネント
- tests/              テストコード

# コーディング規約
- Python: ruff でフォーマット・リント
- TypeScript: ESLint + Prettier
- 型アノテーション必須（Python: mypy --strict, TS: strict mode）

# 禁止事項
- 外部APIキーのハードコード
- tests/ 以外のファイル削除を自律的に行わない
- 設計書にない機能の追加
- 既存テストを削除・無効化しない
```

### 3.3 Design Flow（設計・定義）

実装フローとは完全に独立。

```
要件（人間が指示）
  │
  ▼
claude -p "この要件の機能仕様を書いて"
  │
  ▼
仕様書ドラフト生成（docs/spec/{feature}.md）
  ├── 機能の振る舞い定義（入力・出力・副作用）
  ├── 境界条件・エッジケース
  ├── 前提条件・制約
  └── 受け入れ基準
  │
  ▼
Codex レビュー（対 要件）
  │
  ▼
人間が承認 → status: approved
  │
  ▼
claude -p "この仕様書からテストケースを導出して"
  │
  ▼
テストケース定義（tests/test-cases/{feature}.md）
  ├── 正常系・境界系・異常系
  ├── Phase分け（実装順序）
  └── 重複チェック済み
  │
  ▼
レビュー（対 仕様書）→ 承認
```

#### 仕様書の粒度

独立してテスト・実装できる単位。判断基準：

- この仕様書だけでテストケースが5〜15個程度書けるか
- 他の仕様書を読まなくても実装に着手できるか（依存はインターフェース参照のみ）
- 1〜2日で実装が完了する規模か

```
docs/spec/
├── ingestion/
│   ├── chunk-splitter.md
│   ├── tagger.md
│   ├── embedder.md
│   └── watcher.md
├── agents/
│   ├── generator.md
│   ├── evaluator.md
│   ├── router.md
│   ├── prerequisite-subgraph.md
│   ├── weakness-tagger.md
│   └── summarizer.md
├── api/
│   ├── quiz-session.md
│   ├── topics.md
│   └── progress.md
└── frontend/
    ├── dashboard.md
    ├── quiz-session.md
    ├── weakness-list.md
    └── progress-history.md
```

#### 仕様書のフォーマット

```markdown
---
feature: chunk-splitter
status: draft  # draft | in_review | approved
reviewed_by:
approved_at:
---

## 機能概要
...

## 振る舞い定義
### function_name(args)
- 入力:
- 出力:
- 振る舞い:

## 依存
- embedder: `store(chunks) -> None` を使用

## このコンポーネントが提供するインターフェース
- `split(markdown) -> List[Chunk]`

## 境界条件
...

## 受け入れ基準
- [ ] ...
```

#### テストケースのフォーマット

```markdown
---
feature: chunk-splitter
spec: docs/spec/ingestion/chunk-splitter.md
status: draft  # draft | in_review | approved
---

## テストケース（実装順）

### Phase 1: 最小骨格
1. 空ファイルを渡すと空リストが返る
2. 見出しなしファイルは1チャンクになる

### Phase 2: 分割ロジック
3. H1で分割される
4. H2/H3でネスト分割される
5. H4以下はH3に含まれる

### Phase 3: メタデータ
6. source_pathが付与される
7. headers階層が付与される
8. updated_atが付与される

### Phase 4: 外部連携
9. LLMでタグが付与される
10. ChromaDBに格納され検索できる
11. 3000トークン超は段落分割される
```

### 3.4 Impl Flow（実装）

人間が計画ファイルを書き、狭いスコープでClaude Codeに渡す。

```
人間が計画ファイル作成（plan/current-task.md）
  │
  ▼
ガード（仕様書・テストケースの存在 + 承認チェック）
  │
  ├── 不足 → 中断。Design Flowを先に実行するよう通知
  │
  ▼ 通過
TDDサイクル（テストケースを上から1つずつ）
  │
  ├── レビュー観点注入済みpromptで claude -p 実行
  ├── リンター強制（ゼロ違反）
  ├── セルフレビュー（レビュー観点チェック）
  ├── 違反あれば自律修正
  ├── RED確認 → 実装 → GREEN確認
  │
  ▼
Phase完了ごとにレビュー
  │
  ▼
全Phase完了 → Codex/2体レビュー → 自律修正
```

#### 計画ファイルのフォーマット

```markdown
# plan/current-task.md
---
scope: chunk-splitter
spec: docs/spec/ingestion/chunk-splitter.md
test_cases: tests/test-cases/ingestion/chunk-splitter.md
---

## 今回やること
chunk-splitter の Phase 1（テストケース 1〜3）のみ実装する

## 対象テストケース
1. 空ファイルを渡すと空リストが返る
2. 見出しなしファイルは1チャンクになる
3. H1で分割される

## やらないこと
- Phase 2以降
- メタデータ付与
- LLMタグ付与

## 完了条件
- 上記3つのテストがGREEN
- ruff / mypy がパス
```

#### 実装着手前ガード

```python
async def implementation_guard(feature: str) -> bool:
    spec_path = f"docs/spec/{feature}.md"
    test_cases_path = f"tests/test-cases/{feature}.md"

    spec_exists = os.path.exists(spec_path)
    test_cases_exist = os.path.exists(test_cases_path)

    if not spec_exists:
        print(f"中断: {spec_path} が存在しません")
        return False

    if not test_cases_exist:
        print(f"中断: {test_cases_path} が存在しません")
        return False

    spec = read_frontmatter(spec_path)
    if spec.get("status") != "approved":
        print(f"中断: 仕様書が未承認です（現在: {spec.get('status')}）")
        return False

    return True
```

### 3.5 Review Flow（レビュー体制）

#### レビューの照合関係

```
成果物              レビュー基準
─────────────────────────────────
仕様書          ← 要件（人間の指示）
テストケース    ← 仕様書
テストコード    ← テストケース
実装コード      ← テスト + 仕様書
```

#### レビュー実行体制

```
通常時:
  セルフレビュー → Codex レビュー → 指摘を自律修正

Codex レート制限時:
  セルフレビュー → Claude Agent A ─┐
                                   ├→ 突合 → 自律修正
                   Claude Agent B ─┘
```

#### 突合ロジック

```
Agent A の指摘: [a1, a2, a3]
Agent B の指摘: [b1, b2]

  両方が指摘     → 修正必須
  片方のみ指摘   → 重大度を判定
                    ├── 軽微（振る舞い不変） → 自律修正
                    └── 重大（振る舞い/設計に影響） → 自律修正を試みる
                          ├── 修正後テストGREEN → 完了
                          └── テスト壊れた or 仕様と矛盾 → 人間判断
  両方指摘なし   → LGTM
```

#### 人間にエスカレーションする基準

人間に来るのは以下の場合のみ:

1. 仕様書自体の変更が必要
2. 修正してもテストが通らず、巻き戻しても解決しない
3. 迷走ガード発動（リトライ3回、タイムアウト等）

それ以外は全てAIが自律的に修正して完了する。

---

## 4. ガード機構

### 4.1 迷走検知ガード

プロンプトではなくハーネスで機械的に強制。

| シグナル | 閾値 | 意味 |
|:---|:---|:---|
| 同一テストのリトライ回数 | 3回 | 同じテストを通せずループ |
| テスト失敗のエラーメッセージ重複 | 3回連続同一 | 同じアプローチの繰り返し |
| 1タスクの経過時間 | 15分 | 小スコープに時間かかりすぎ |
| ファイル変更の巻き戻し | 2回 | 書いては消しての繰り返し |
| diffの行数 | 計画スコープの3倍超 | スコープ外への逸脱 |

#### エスカレーション段階

```
迷走シグナル検知
  │
  ▼
Level 1: Claude Code に別アプローチ指示（自動）
  │
  ▼ 解決しない
Level 2: Codex に相談（レート制限時はスキップ）
  │
  ├── 助言あり → 助言で再試行
  └── レート制限 → スキップ
  │
  ▼ 解決しない
Level 3: 人間にエスカレーション（中断・通知）
```

### 4.2 リンター強制ガード

全ての claude -p 呼び出し後に自動実行。AIの判断を介在させない。

```
コード変更
  │
  ▼
ruff check + ruff format + mypy --strict（Python）
eslint --max-warnings 0 + prettier（TypeScript）
  │
  ├── エラー 0 + 警告 0 → 次のステップへ
  └── 違反あり → 全件修正（差分かどうか問わない）→ 再チェック
      → 3回修正しても残る → 迷走ガード発動
```

**「自分の変更箇所ではない」という判断をさせない。ファイル内の全指摘を対象にする。**

---

## 5. オブザーバビリティ

### 5.1 ログ構造

```
logs/
├── 2026-05-01T10-30-00_chunk-splitter/
│   ├── harness.jsonl        # ハーネスのイベントログ
│   ├── claude-code.log      # claude -p の入出力全文
│   └── codex-review.log     # Codexレビューの入出力全文
```

### 5.2 記録するイベント

```jsonl
{"ts":"...","event":"guard_check","spec":"chunk-splitter.md","result":"pass"}
{"ts":"...","event":"tdd_start","test_case":"空ファイル → 空リスト"}
{"ts":"...","event":"claude_p_call","prompt":"テストを書いて...","tokens_in":1200,"tokens_out":400}
{"ts":"...","event":"test_run","result":"RED","failures":1}
{"ts":"...","event":"test_run","result":"GREEN","passed":1}
{"ts":"...","event":"lint_violations","errors":2,"warnings":1}
{"ts":"...","event":"lint_passed","attempt":2}
{"ts":"...","event":"self_review","violations":1,"details":["マジックナンバー検出"]}
{"ts":"...","event":"review_start","reviewer":"codex"}
{"ts":"...","event":"codex_rate_limited","fallback":"dual_claude"}
{"ts":"...","event":"claude_review","agent":"A","issues":["...","..."]}
{"ts":"...","event":"claude_review","agent":"B","issues":["..."]}
{"ts":"...","event":"review_reconciled","agreed":1,"disputed":1}
{"ts":"...","event":"disputed_issue_assessed","issue":"...","severity":"minor","action":"auto_fix"}
{"ts":"...","event":"drift_detected","metric":"retry_count","value":4}
{"ts":"...","event":"escalation_to_human","reason":"..."}
```

---

## 6. レビュー観点

### 6.1 共通観点（common）

```markdown
## 命名規則
- 変数名は省略しない（usr → user, msg → message）
- 定数は UPPER_SNAKE_CASE
- 関数名は動詞始まり（get_, create_, update_, delete_, is_, has_）
- bool変数は is_/has_/can_ プレフィクス
- クラス名は PascalCase、名詞

## マジックナンバー/ストリング禁止
- 数値リテラルは定数に切り出す
- 文字列リテラルも繰り返し使う場合は定数化
- OK: WEAKNESS_THRESHOLD = 60
- NG: if score < 60

## 関数設計
- 30行以内
- 引数4つ以内（超える場合は構造体に）
- 1関数1責務（関数名にandが要るなら分割）

## エラーハンドリング
- エラーメッセージに具体的文脈を含める
- 握り潰し禁止

## テスト品質
- 1テスト1関心事（関連するassertはまとめてよい）
- テスト名は何を検証しているかわかる命名
- test_{対象}_{条件}_{期待結果} 形式
- setupの重複はfixture/helper化
- モックは外部依存のみ
```

### 6.2 バックエンド固有（backend）

```markdown
## Python固有
- bare except 禁止（except Exception as e を使う）
- f-string推奨（.format() や % は使わない）
- dataclass or TypedDict で構造化（dictの直接操作を避ける）
- async関数とsync関数を混在させない

## FastAPI固有
- エンドポイントは必ずレスポンスモデルを定義
- 依存注入（Depends）でDB接続等を渡す
- HTTPExceptionで適切なステータスコードを返す

## LangGraph/LangChain固有
- StateのフィールドはTypedDictで型定義
- プロンプトはハードコードせず定数or外部ファイル管理
- LLM呼び出し結果は構造化パース（JSON mode）
```

### 6.3 フロントエンド固有（frontend）※Phase 3で詳細化

```markdown
## React固有
- コンポーネントは関数コンポーネントのみ
- Props は type 定義必須
- useEffect の依存配列は省略禁止
- children の暗黙的バケツリレー禁止

## shadcn/ui固有
- コンポーネントカスタマイズは cn() ユーティリティで
- Radix UIのプリミティブを直接使わない（shadcn/uiのラッパーを通す）

## Tailwind固有
- インラインstyle禁止（Tailwindクラスを使う）
- マジックカラー禁止（テーマトークンを使う）
- 長いクラス列は clsx/cn で整理
- @apply は原則使わない

## Jotai固有
- atom定義はfeature単位のファイルにまとめる
- 派生atomで計算ロジックを表現（コンポーネント内で計算しない）
```

### 6.4 注入タイミング

```
実装前注入（claude -p プロンプトに同梱）
  → 「これを守ってコードを書け」
  → 対象: 命名、マジックナンバー、関数設計、エラーハンドリング

セルフレビュー（実装完了後、Codexレビュー前に自動実行）
  → 「守れたかチェックしろ」
  → 対象: 上記全て（同じ観点を2回チェック）

Codex / 2体レビュー
  → 仕様整合性・設計判断・見落としに集中
```

---

## 7. 開発フェーズ

### Step 0: ハーネス構築

開発基盤を先に整備する。

1. CLAUDE.md 作成（静的制約）
2. Agent SDKハーネス構築
   - Design Flow
   - Impl Flow（ガード含む）
   - Review Flow（Codexフォールバック含む）
3. レビュー観点をファイル化（.harness/review-criteria-*.md）
4. リンター設定（pyproject.toml / eslint.config.js）
5. ログ基盤

### Step 1: RAGパイプライン + Tagger（2週間）

Design Flow → Impl Flow の順で以下を実装:

- chunk-splitter: Markdown → チャンク分割
- tagger: LLMによるタグ付与
- embedder: チャンク → embedding → ChromaDB格納
- watcher: ファイル監視 → 差分検知
- 検索精度検証: 見出し単位 vs 固定長の比較

### Step 2: LangGraphエージェント（2週間）

- generator: RAG検索 + 問い生成
- evaluator: 回答評価 + 理解度判定
- router: 次アクション判断
- prerequisite-subgraph: 前提知識の再帰サブセッション（max_depth=2）
- weakness-tagger: 弱点タグ付与 + Obsidian書き戻し
- summarizer: セッション終了サマリー

### Step 3: FastAPI + React GUI（2週間）

- APIエンドポイント実装
- フロントエンド画面実装（ダッシュボード、クイズ、弱点一覧、進捗）
- フロントエンドレビュー観点の詳細化

### Step 4: 精度チューニング + 仕上げ（1週間）

- チャンク分割の精度評価（Recall@K）
- LLM採点の一貫性検証
- 再帰サブセッションのUXテスト
- エッジケース対応
