---
status: ready
---

# テストケース一覧

# 検証焦点

| テストの種類 | 検証焦点 |
|---|---|
| Phase 1（最小骨格） | 公開 `TypedDict` DTO 形状、`request/get` の最小正常系、worker 成功時の全必須連携 |
| Phase 2（コアロジック） | 入力検証、共有 retry 予算、LLM 応答バリデーション、検証済み DTO から B3 保存入力への変換 |
| Phase 3（エッジケース） | 境界条件、同一 topic 再受付、状態更新失敗時の停止位置、優先送出例外 |
| Phase 4（外部連携） | 依存 Protocol の例外伝播、`mark_failed()` 連携、非再送出条件、非エラー確認 |

## Phase 1: 最小骨格

| ID | 観点 | 前提 | 入力 | 期待結果 | 備考 |
|---|---|---|---|---|---|
| TC-01 | `roadmap_generation_types.py` の公開 DTO / 失敗コード / 例外階層 / 内部 DTO 契約を静的に検証する | 型定義を参照できる | `backend/roadmap/infrastructure/roadmap_generation_types.py` の公開定義を確認する | `RoadmapGenerationAccepted` は `TypedDict` であり `job_id: UUID` と `status: Literal["queued"]` だけを持つ。`RoadmapGenerationQueuedStatus` / `RunningStatus` / `CompletedStatus` / `FailedStatus` は状態別 `TypedDict` として定義され、`RoadmapGenerationJobStatus` はその 4 型 union と完全一致する。`CompletedStatus` は `roadmap_id` 必須、`FailedStatus` は `error_code` / `error_message` 必須である。`RoadmapGenerationFailureCode` は `Literal["schedule_failed", "llm_request_failed", "llm_json_parse_failed", "llm_schema_validation_failed", "persistence_failed"]` と完全一致し、`FailedStatus.error_code` と `RoadmapGenerationJobStatusStore.mark_failed()` の両方で同じ型が使われる。公開 DTO は属性アクセス前提ではなく辞書互換値として扱う契約である。例外階層は `RoadmapGenerationError(Exception)` を基底とし、`RoadmapGenerationInputError`、`RoadmapGenerationScheduleError`、`RoadmapGenerationLlmError`、`RoadmapGenerationJobStoreError`、`RoadmapGenerationJobNotFoundError` がその直接子クラス、`RoadmapGenerationLlmResponseError(RoadmapGenerationError)` の子として `RoadmapGenerationJsonParseError` と `RoadmapGenerationSchemaValidationError` が定義されていることを `issubclass` で検証する。`ValidatedRoadmapGeneration` と `ValidatedRoadmapGenerationItem` が frozen dataclass として定義されていることを検証する | 型定義・例外階層・内部 DTO の最小骨格 |
| TC-02 | `request_roadmap_generation` 正常系で受付 DTO 形状、呼び出し順、topic 原値保持を検証する | `job_id_generator` / `job_store` / `scheduler` の呼び出し順と引数を検査できる Protocol 準拠テストダブルを使う | `topic="  TypeScript  "`、`job_id_generator.generate()` は `aaaaaaaa-aaaa-aaaa-aaaa-aaaaaaaaaaaa` を返す | 戻り値は辞書互換で `{"job_id": UUID("aaaaaaaa-aaaa-aaaa-aaaa-aaaaaaaaaaaa"), "status": "queued"}` と完全一致する。`roadmap_id` / `error_code` / `error_message` は含まれない。`job_id_generator.generate()` はちょうど 1 回、`job_store.create_queued_job(job_id, topic)` はちょうど 1 回、`scheduler.enqueue_roadmap_generation(job_id, topic)` はちょうど 1 回呼ばれる。呼び出し順は `generate()` → `create_queued_job()` → `enqueue_roadmap_generation()` の順で完全一致する。`topic` は `strip()` された値ではなく入力文字列そのままが `create_queued_job()` と `enqueue_roadmap_generation()` に渡される。ログ: `roadmap_generation_accepted` イベントが info レベルでちょうど 1 件記録され、`job_id` と `topic` を含む | 受付 API の主正常系 |
| TC-03 | `get_roadmap_generation_job` が 4 状態をそのまま返し、状態ごとの返却形状を固定する | `job_store.get_job()` の返却値を制御できる | 同一 `job_id` に対して `queued`、`running`、`completed(roadmap_id=bbbbbbbb-bbbb-bbbb-bbbb-bbbbbbbbbbbb)`、`failed(error_code="llm_request_failed", error_message="timeout")` の 4 パターンで実施する | 各パターンで `job_store.get_job(job_id)` がちょうど 1 回だけ呼ばれ、戻り値は store が返した辞書互換 DTO と完全一致する。`queued` / `running` は `status` だけを持ち、`roadmap_id` / `error_code` / `error_message` を含まない。`completed` は `status="completed"` と `roadmap_id` だけを持ち、`error_code` / `error_message` を含まない。`failed` は `status="failed"`、`error_code`、`error_message` を持ち、`roadmap_id` を含まない | 状態観測 API の最小正常系 |
| TC-04 | worker 成功系で `mark_running`、LLM 生成、検証済み DTO 化、`clock.now()`、B3 保存、`mark_completed` 連携を検証する | `llm_client` / `persistence` / `job_store` / `clock` について、呼び出し回数・引数・後続非呼び出しを検査できる Protocol 準拠テストダブルを使う | `job_id=cccccccc-cccc-cccc-cccc-cccccccccccc`、`topic="  TypeScript  "` として必須内部 worker entry `_run_roadmap_generation_job(job_id, topic, *, llm_client, persistence, job_store, clock)` を実行する。LLM は次の妥当 JSON を返す: `{"topic":"  TypeScript  ","items":[{"title":"基礎","description":"初学者が押さえるべきTypeScriptの基本事項","level":"major","children":[{"title":"関数","description":"関数宣言と型注釈を学ぶ","level":"middle","children":[{"title":"関数の型注釈","description":"引数と返り値の型を書く","level":"detail","children":[]},{"title":"オーバーロード","description":"複数シグネチャを定義する","level":"detail","children":[]}]},{"title":"型システム","description":"値に対する型の表現を学ぶ","level":"middle","children":[{"title":"ユニオン型","description":"複数候補の型を扱う","level":"detail","children":[]},{"title":"型推論","description":"注釈なしで推論される型を理解する","level":"detail","children":[]}]}]},{"title":"応用","description":"中上級者へのステップアップに必要な知識","level":"major","children":[{"title":"型レベルプログラミング","description":"高度な型操作を学ぶ","level":"middle","children":[{"title":"条件型","description":"条件に応じて型を分岐させる","level":"detail","children":[]},{"title":"mapped types","description":"既存型から新しい型を導く","level":"detail","children":[]}]}]}]}`。`clock.now()` は `2026-05-21T12:34:56+09:00`、`persistence.save_roadmap(...)` は `roadmap_id=dddddddd-dddd-dddd-dddd-dddddddddddd` を返す | `_run_roadmap_generation_job(...)` は `job_store.mark_running(job_id)` を最初に 1 回呼ぶ。`llm_client.generate_roadmap_json(topic)` はちょうど 1 回呼ばれ、`topic` には入力文字列そのままが渡る。`clock.now()` は保存 1 回につきちょうど 1 回だけ呼ばれる。`persistence.save_roadmap(...)` はちょうど 1 回呼ばれ、引数 `RoadmapSaveInput` の `topic` は入力 topic と完全一致し、`created_at` は `clock.now()` の返り値と完全一致し、`items` は raw JSON 文字列ではなく検証済み内部 DTO から変換された値である。`items` の検証は厳密な serialization 形状や追加フィールド禁止ではなく、保存引数から抽出できる必須学習項目情報で行い、ルート sibling 順序 `["基礎", "応用"]`、`基礎` 配下 middle 順序 `["関数", "型システム"]`、`関数` 配下 detail 順序 `["関数の型注釈", "オーバーロード"]`、`型システム` 配下 detail 順序 `["ユニオン型", "型推論"]`、`応用` 配下 middle 順序 `["型レベルプログラミング"]`、`型レベルプログラミング` 配下 detail 順序 `["条件型", "mapped types"]` が入力 JSON と完全一致で保持され、モジュール側で並べ替えないことを確認する。各項目について `title` / `description` / `level` / `children` に表現された必須学習項目情報が入力 JSON から欠落なく引き継がれることを確認する。`job_store.mark_completed(job_id, roadmap_id)` はちょうど 1 回呼ばれ、`roadmap_id` は persistence 返却値と完全一致する。`job_store.mark_failed(...)` は 0 回で、worker は非例外で `None` を返す。ログ: `roadmap_generation_job_started` イベントが info レベルで 1 件（`job_id`, `topic` 含む）、`roadmap_generation_job_completed` イベントが info レベルで 1 件（`job_id`, `roadmap_id` 含む）記録される。`roadmap_generation_job_failed` と `roadmap_generation_llm_retry` は 0 件である | 成功時の end-to-end 最小骨格 |

## Phase 2: コアロジック

| ID | 観点 | 前提 | 入力 | 期待結果 | 備考 |
|---|---|---|---|---|---|
| TC-10 | `topic.strip()` が空文字なら受付前に `RoadmapGenerationInputError` を送出する | `job_id_generator` / `job_store` / `scheduler` の非呼び出しを検査できる | `topic=""` と `topic="  \n\t  "` の 2 パターンで `request_roadmap_generation(...)` を呼ぶ | 各パターンで `RoadmapGenerationInputError` を送出する。`job_id_generator.generate()`、`job_store.create_queued_job()`、`scheduler.enqueue_roadmap_generation()` はいずれも 0 回である。例外 message は仕様未規定のため検証対象外とする。ログ: `roadmap_generation_input_rejected` イベントが warning レベルでちょうど 1 件記録され、`error_type` を含む。`roadmap_generation_accepted` イベントは 0 件である | 入力契約の主境界 |
| TC-11 | JSON パース失敗とスキーマ違反が単一の retry 予算 3 回を共有し、3 回目成功なら継続する | `llm_client` の返却値を呼び出しごとに切り替えられ、`job_store` / `persistence` / `clock` について呼び出し回数・引数・後続非呼び出しを検査できる Protocol 準拠テストダブルを使う | 必須内部 worker entry `_run_roadmap_generation_job(job_id, topic, *, llm_client, persistence, job_store, clock)` の実行で、1 回目は JSON パース不能文字列、2 回目は `topic` 不一致のスキーマ違反 JSON、3 回目は妥当 JSON を返す | `llm_client.generate_roadmap_json(topic)` はちょうど 3 回呼ばれる。パース失敗とスキーマ違反で別予算に分かれず、合計 3 回目で成功扱いになる。`job_store.mark_failed(...)` は 0 回、`persistence.save_roadmap(...)` は妥当 JSON 取得後にちょうど 1 回、`job_store.mark_completed(...)` はちょうど 1 回呼ばれる。worker は非例外で `None` を返す。ログ: `roadmap_generation_llm_retry` イベントが warning レベルで 2 件記録され、各イベントに `job_id`, `attempt`, `error_type` を含む。`roadmap_generation_job_failed` は 0 件である | 共有 retry 予算の正常系 |
| TC-12 | retry 予算を使い切り、最後の失敗が JSON パース失敗なら `llm_json_parse_failed` で failed 化する | `llm_client` の返却値を呼び出しごとに切り替えられ、`job_store.mark_failed()` の引数、`persistence.save_roadmap()` / `job_store.mark_completed()` の後続非呼び出しを検査できる Protocol 準拠テストダブルを使う | 必須内部 worker entry `_run_roadmap_generation_job(job_id, topic, *, llm_client, persistence, job_store, clock)` の実行で、1 回目は `topic` 不一致 JSON、2 回目は未知 `level` JSON、3 回目は JSON パース不能文字列を返す | `llm_client.generate_roadmap_json(topic)` はちょうど 3 回呼ばれる。最後の失敗カテゴリが JSON パース失敗であるため、`job_store.mark_failed(job_id, "llm_json_parse_failed", error_message)` がちょうど 1 回呼ばれる。`error_message` は 3 回目パース失敗の理由を含む。`persistence.save_roadmap(...)` と `job_store.mark_completed(...)` は 0 回である。`mark_failed()` 成功時、worker は元のパース例外を再送出せず `None` を返す。ログ: `roadmap_generation_llm_retry` が 2 件（1,2 回目の失敗分）、`roadmap_generation_job_failed` が 1 件（`error_code="llm_json_parse_failed"` 含む）記録される | retry 枯渇時のカテゴリ決定 |
| TC-13 | スキーマ違反パターンがすべて retry 対象であり、最後がスキーマ違反なら `llm_schema_validation_failed` で failed 化する | `llm_client` に各スキーマ違反 JSON を個別に与えられ、`job_store.mark_failed()` の引数、`persistence.save_roadmap()` / `job_store.mark_completed()` の後続非呼び出しを検査できる Protocol 準拠テストダブルを使う | 必須内部 worker entry `_run_roadmap_generation_job(job_id, topic, *, llm_client, persistence, job_store, clock)` の実行で、次の固定サブケースをそれぞれ独立に実施し、各サブケースでは 3 回とも同一の違反種別 JSON を返す: 1. ルート必須欠落-`topic` 欠落、2. ルート必須欠落-`items` 欠落、3. ルート余剰フィールド-root に `extra` を追加、4. `items` 構造違反-`items` を 1 件配列にする、5. 項目必須欠落-1 件目 `major` から `description` を欠落させる、6. 項目余剰フィールド-1 件目 `major` に `extra` を追加する、7. 未知 `level`-`middle.level="minor"` にする、8. 親子関係違反-`major` 直下に `detail` を置く、9. `detail.children != []`-`detail.children` に 1 件子要素を入れる、10. 型不一致-`title` を数値にする、11. 型不一致-`description` を配列にする、12. 型不一致-`children` を文字列にする、13. `topic` 不一致-root `topic` を入力 topic と別文字列にする | 各サブケースで `llm_client.generate_roadmap_json(topic)` はちょうど 3 回呼ばれる。各違反は JSON パース失敗と共有の retry 対象として扱われ、`job_store.mark_failed(job_id, "llm_schema_validation_failed", error_message)` がちょうど 1 回呼ばれる。`error_message` は各サブケース 3 回目の違反理由を含む。`persistence.save_roadmap(...)` と `job_store.mark_completed(...)` は 0 回である。`mark_failed()` 成功時、worker は元の検証例外を再送出せず `None` を返す | LLM 応答バリデーションの主検証 |
| TC-14 | 妥当 JSON から B3 保存入力へ変換するとき、topic 原値・許可階層・必須学習項目情報・空枝許容を保持する | `persistence.save_roadmap(...)` 引数の内容を詳細比較でき、`job_store.mark_completed(...)` に渡る値を検査できる Protocol 準拠テストダブルを使う | 必須内部 worker entry `_run_roadmap_generation_job(job_id, topic, *, llm_client, persistence, job_store, clock)` を `topic="TypeScript"` で実行する。LLM は次の妥当 JSON を返す: `{"topic":"TypeScript","items":[{"title":"基礎","description":"TypeScript学習の土台を固める","level":"major","children":[{"title":"関数","description":"関数定義と型付けの基礎を学ぶ","level":"middle","children":[{"title":"関数宣言","description":"function 構文で関数を定義する","level":"detail","children":[]},{"title":"引数の型注釈","description":"引数へ明示的に型を書く","level":"detail","children":[]},{"title":"戻り値の型注釈","description":"戻り値へ明示的に型を書く","level":"detail","children":[]}]},{"title":"実行環境","description":"TypeScriptコードを動かす準備を整える","level":"middle","children":[]}]},{"title":"応用","description":"発展的な内容へ進む前提を整える","level":"major","children":[]}]}` | `persistence.save_roadmap(...)` に渡される `RoadmapSaveInput.topic` は `"TypeScript"` と完全一致する。`items` は raw JSON をそのまま渡した値ではなく、検証済み内部 DTO を経由した値であり、`items` の検証対象は `title` / `description` / `level` / `children` に表現された必須学習項目情報と sibling 順序の引き継ぎに限定する。ルート sibling 順序 `["基礎", "応用"]`、`基礎` 配下 middle 順序 `["関数", "実行環境"]`、`関数` 配下 detail 順序 `["関数宣言", "引数の型注釈", "戻り値の型注釈"]` が入力 JSON と完全一致で保持される。`応用.children=[]` による `major.children=[]` と `実行環境.children=[]` による `middle.children=[]` はどちらも妥当な空枝として保存まで通る。`基礎`、`関数`、`関数` 配下 detail 各項目、`実行環境`、`応用` の各要素で `title` / `description` / `level` / `children` が入力 JSON から欠落なく引き継がれる。`major -> middle -> detail` 以外の階層は含まれない。B3 所有の追加フィールド有無や厳密 serialization 形状は検証対象外とする。ロードマップ本体は job 完了 DTO に入らず、`job_store.mark_completed(job_id, roadmap_id)` には `roadmap_id` だけが渡される | B3 連携変換の内容検証 |

## Phase 3: エッジケース

| ID | 観点 | 前提 | 入力 | 期待結果 | 備考 |
|---|---|---|---|---|---|
| TC-20 | `create_queued_job()` 失敗はラップせず送出し、enqueue しない | `job_id_generator` / `job_store` / `scheduler` について、呼び出し回数・引数・後続非呼び出しを検査できる Protocol 準拠テストダブルを使い、`job_store.create_queued_job()` が `RoadmapGenerationJobStoreError` を送出する | `request_roadmap_generation(topic="TypeScript", ...)` | `RoadmapGenerationJobStoreError` をそのまま送出する。`scheduler.enqueue_roadmap_generation()` は 0 回である。`job_id_generator.generate()` は入力検証通過後なのでちょうど 1 回呼ばれる | 受付時 store 障害 |
| TC-21 | `create_queued_job()` 成功後に enqueue が失敗したとき、`schedule_failed` を記録できた場合だけ元の `RoadmapGenerationScheduleError` を再送出する | `job_id_generator` / `scheduler` / `job_store` について、呼び出し回数・引数・後続非呼び出しを検査できる Protocol 準拠テストダブルを使う。`scheduler.enqueue_roadmap_generation()` が例外送出し、`job_store.mark_failed()` は成功する | `request_roadmap_generation(topic="TypeScript", ...)`。`scheduler.enqueue_roadmap_generation()` は `RoadmapGenerationScheduleError("queue down")` を送出する | `job_store.mark_failed(job_id, "schedule_failed", "queue down")` がちょうど 1 回呼ばれる。`request_roadmap_generation(...)` は元の `RoadmapGenerationScheduleError` をそのまま再送出する。受付成功扱いにはならず、戻り値は返らない。ジョブ削除、再 enqueue、補償処理は行わない。ログ: `roadmap_generation_schedule_failed` イベントが error レベルで 1 件記録され、`job_id` と `error_type` を含む。`roadmap_generation_accepted` は 0 件である | enqueue 失敗の主分岐 |
| TC-22 | enqueue 失敗後の `mark_failed()` が store 例外なら、その store 例外を優先送出する | `job_id_generator` / `scheduler` / `job_store` について、呼び出し回数・引数を検査できる Protocol 準拠テストダブルを使う。`scheduler.enqueue_roadmap_generation()` が `RoadmapGenerationScheduleError`、`job_store.mark_failed()` が `RoadmapGenerationJobStoreError` を送出する | `request_roadmap_generation(topic="TypeScript", ...)` | `job_store.mark_failed(job_id, "schedule_failed", str(schedule_exc))` を 1 回試みたうえで、`RoadmapGenerationJobStoreError` を優先送出する。元の `RoadmapGenerationScheduleError` は再送出しない | 優先送出例外 |
| TC-23 | worker 開始時の `mark_running()` 失敗では後続処理を一切行わない | `job_store` / `llm_client` / `persistence` について、呼び出し回数・後続非呼び出しを検査できる Protocol 準拠テストダブルを使い、`job_store.mark_running()` が `RoadmapGenerationJobStoreError` を送出する | 必須内部 worker entry `_run_roadmap_generation_job(job_id, topic, *, llm_client, persistence, job_store, clock)` を実行する | `RoadmapGenerationJobStoreError` をそのまま送出する。`llm_client.generate_roadmap_json()`、`persistence.save_roadmap()`、`job_store.mark_failed()` はいずれも 0 回である | 開始直後停止 |
| TC-24 | 保存成功後の `mark_completed()` 失敗はそのまま送出し、巻き戻しや failed 化を行わない | `llm_client` / `persistence` / `job_store` について、呼び出し回数・引数・追加の後続非呼び出しを検査できる Protocol 準拠テストダブルを使う。`persistence.save_roadmap(...)` は成功し、`job_store.mark_completed()` が `RoadmapGenerationJobStoreError` を送出する | 必須内部 worker entry `_run_roadmap_generation_job(job_id, topic, *, llm_client, persistence, job_store, clock)` に妥当 JSON を与える | `persistence.save_roadmap(...)` はちょうど 1 回成功し、`job_store.mark_completed(job_id, roadmap_id)` を 1 回試みたうえで `RoadmapGenerationJobStoreError` をそのまま送出する。追加の `job_store.mark_failed()`、保存済みロードマップの巻き戻し、再保存は行わない | 完了反映失敗 |
| TC-25 | 同一 topic を 2 回受け付けても別ジョブとして扱う | `job_id_generator.generate()` が 2 個の異なる UUID を返す | 同じ `topic="TypeScript"` で `request_roadmap_generation(...)` を 2 回呼ぶ | 2 回とも正常に `RoadmapGenerationAccepted` を返し、`job_id` は互いに異なる。`job_store.create_queued_job()` と `scheduler.enqueue_roadmap_generation()` はそれぞれ topic 同一のまま 2 回呼ばれ、既存ジョブの上書きや重複排除は行わない | 同一 topic 再受付の境界 |

## Phase 4: 外部連携

| ID | 観点 | 前提 | 入力 | 期待結果 | 備考 |
|---|---|---|---|---|---|
| TC-30 | `get_roadmap_generation_job()` の `get_job()` 例外をラップせず送出する | `job_store.get_job()` の例外を切り替えられる | 1. 未知 `job_id` で `RoadmapGenerationJobNotFoundError`、2. 取得失敗で `RoadmapGenerationJobStoreError` を送出する 2 パターンで実施する | 1 は `RoadmapGenerationJobNotFoundError`、2 は `RoadmapGenerationJobStoreError` をそれぞれそのまま送出する。追加の変換や別例外へのラップは行わない | 状態取得 API の例外契約 |
| TC-31 | `RoadmapGenerationLlmError` は retry せず `llm_request_failed` で failed 化し、`mark_failed()` 成功時は元例外を再送出しない | `llm_client` / `job_store` / `persistence` について、呼び出し回数・引数・後続非呼び出しを検査できる Protocol 準拠テストダブルを使う。`llm_client.generate_roadmap_json()` が例外送出し、`mark_failed()` 成功パターンと store 失敗パターンを分けて実施する | 必須内部 worker entry `_run_roadmap_generation_job(job_id, topic, *, llm_client, persistence, job_store, clock)` の実行で `llm_client.generate_roadmap_json(topic)` が `RoadmapGenerationLlmError("timeout")` を送出する | 成功パターンでは `llm_client.generate_roadmap_json(topic)` はちょうど 1 回だけ呼ばれ、retry されない。`job_store.mark_failed(job_id, "llm_request_failed", "timeout")` がちょうど 1 回呼ばれ、`persistence.save_roadmap()` と `job_store.mark_completed()` は 0 回、worker は元の `RoadmapGenerationLlmError` を再送出せず `None` を返す。ログ: `roadmap_generation_job_failed` が 1 件（`error_code="llm_request_failed"` 含む）記録される。`roadmap_generation_llm_retry` は 0 件である。store 失敗パターンでは `job_store.mark_failed(...)` 実行時の `RoadmapGenerationJobStoreError` を優先送出し、元の `RoadmapGenerationLlmError` は再送出しない | LLM 呼び出し失敗 |
| TC-32 | persistence 失敗は `persistence_failed` で failed 化し、`mark_failed()` 成功時は元例外を再送出しない | `llm_client` / `persistence` / `job_store` について、呼び出し回数・引数・後続非呼び出しを検査できる Protocol 準拠テストダブルを使う。`persistence.save_roadmap(...)` が例外送出し、`RoadmapPersistenceWriteError` と `RoadmapPersistenceInputError` の両方を実施し、各々 `mark_failed()` 成功 / store 失敗を分けて確認する | 必須内部 worker entry `_run_roadmap_generation_job(job_id, topic, *, llm_client, persistence, job_store, clock)` の実行で妥当 JSON 取得後、`persistence.save_roadmap(...)` が 1. `RoadmapPersistenceWriteError("db locked")`、2. `RoadmapPersistenceInputError("invalid title")` を送出する | 各パターンで `job_store.mark_failed(job_id, "persistence_failed", str(exc))` がちょうど 1 回呼ばれる。`mark_failed()` 成功時、worker は元の persistence 例外を再送出せず `None` を返す。`mark_failed()` が `RoadmapGenerationJobStoreError` を送出した場合はその store 例外を優先送出し、元の persistence 例外は再送出しない。retry は行わない | B3 保存失敗 |

# 網羅性チェック

仕様書の受け入れ基準の各項目に対応するテストケース ID を記載する。
全項目が少なくとも1つのテストケースでカバーされていることを確認する。

| 受け入れ基準 | 対応テストケース |
|---|---|
| `request_roadmap_generation` が `job_id` を含む `RoadmapGenerationAccepted` を返す | TC-02 |
| `RoadmapGenerationAccepted` / `RoadmapGenerationJobStatus` は `backend/roadmap/infrastructure/roadmap_generation_types.py` 上の `TypedDict` 公開 DTO として定義され、呼び出し側は `result["status"]` のようにキー参照で扱う | TC-01, TC-02, TC-03 |
| `RoadmapGenerationFailureCode` は `Literal["schedule_failed", "llm_request_failed", "llm_json_parse_failed", "llm_schema_validation_failed", "persistence_failed"]` に固定され、failed DTO と `job_store.mark_failed()` が同じ型を共有する | TC-01 |
| `get_roadmap_generation_job` で `queued` / `running` / `completed` / `failed` を取得できる | TC-03 |
| `completed` 状態でのみ `roadmap_id` が返り、`failed` 状態でのみ `error_code` / `error_message` が返り、ロードマップ本体の取得は既存 B5 retrieval に委譲される | TC-03, TC-14 |
| トピック名から LLM がロードマップを生成する | TC-04 |
| 大枠が「基礎」「応用」の 2 段階で生成される | TC-04, TC-14 |
| 許可する階層と親子関係は `major -> middle -> detail` に固定され、`major.children=[]` と `middle.children=[]` を許容する | TC-13, TC-14 |
| 各項目に `title` と `description` が含まれる | TC-04, TC-14 |
| `major.children` と `middle.children` の sibling 順序は LLM 応答配列順のまま保持され、generation モジュール側で並べ替えない | TC-04, TC-14 |
| `create_queued_job()` 失敗は `RoadmapGenerationJobStoreError` をそのまま送出し、`create_queued_job()` 後の enqueue 失敗は `schedule_failed` を記録できたときだけ `RoadmapGenerationScheduleError` を再送出し、`mark_failed()` 失敗時は `RoadmapGenerationJobStoreError` を優先送出する | TC-20, TC-21, TC-22 |
| LLM 応答の JSON パース失敗とスキーマ違反は総試行回数最大 3 回（初回 1 回 + 再試行 2 回）の単一 retry 予算を共有し、失敗時は最後の失敗カテゴリに応じた `error_code` で job が `failed` になる | TC-11, TC-12, TC-13 |
| `RoadmapGenerationLlmError` と persistence 失敗は generation モジュール内で再試行されず、対応する `mark_failed()` 成功時は worker が元例外を再送出せず終了し、`mark_failed()` 失敗時は `RoadmapGenerationJobStoreError` を優先送出する | TC-31, TC-32 |
| `get_job()` / `mark_running()` / `mark_completed()` の `RoadmapGenerationJobStoreError` は generation モジュールでラップされずそのまま送出される | TC-23, TC-24, TC-30 |
| generation モジュールが検証済み内部 DTO を B3 `RoadmapSaveInput` に変換して RDB 永続化を呼び、`topic`・階層・必須学習項目情報・sibling 順序を引き継ぐ | TC-04, TC-14 |
| 例外階層が `RoadmapGenerationError` 基底で統一され、内部バリデーション例外が `RoadmapGenerationLlmResponseError` の子として定義される | TC-01 |
| `structlog` によるログが各経路で所定のイベントを記録する | TC-02, TC-04, TC-10, TC-11, TC-12, TC-21, TC-31 |

# 記述ルール

- 仕様書に明記された振る舞いだけを前提にする
- 仕様未記載の振る舞いを期待結果に置く場合は `[要仕様追記]` タグを付ける
- 正常系・境界系・異常系が重複なく網羅されていることを確認する
- 仕様書の受け入れ基準の各項目に対応するテストケースが最低1つあることを確認する
- 期待結果に「含まれる」「完全一致」「少なくとも1件」「ちょうど1件」などの検証粒度を明示する
- ログ検証を含む場合は、検証対象イベント名、必須キー、期待値、件数制約、追加キー許容の有無を明示する
- 例外検証を含む場合は、例外型、message、cause chain、ログ記録有無のうち何を確認するかを明示する
- テストが「存在確認」なのか「内容検証」なのかを期待結果で区別して書く
- テストケース文書に書いていない検証を impl レビューで追加要求しなくて済む粒度まで、期待結果を具体化する
